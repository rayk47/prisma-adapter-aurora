# name: Publish Package to NPM
# on:
#   # release:
#   #   types: [published]
#   push
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       id-token: write
#     steps:
#       - uses: actions/checkout@v4
#       # Setup .npmrc file to publish to npm
#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20.x"
#           registry-url: "https://registry.npmjs.org"
#       - run: npm install
#       - run: nx build @raymondjkelly/aurora-prisma-adapter
#       - run: cd ./packages/aurora-prisma-adapter && npm publish --access public
#         env:
#           NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
